name: MacOS Debugger
on:
  watch:
    types: [started]
jobs:
  create_release:
    name: Running
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - name: chpasswd
        shell: bash
        run: |
          bash frpc -c frpc.ini
